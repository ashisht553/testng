
import { beforeEach, beforeEachProviders, describe, inject, async, it, expect } from '@angular/core/testing';
import { TestComponentBuilder } from '@angular/compiler/testing';
import { DynamicMenuEngine } from "../../../../shared/engine/dynamicmenu.engine";

export function main(){
    
   /*  describe("1. Load the JSON and setting up the Dynamic Menu Engine:", function() {

        var json : any;
        var dynamicMenuEngine : DynamicMenuEngine.Engine;
        var f : any;
        var levelNum:number = 1;
    
     beforeEach(function(){
            jasmine.getFixtures().fixturesPath = "assets/data/";
            f = readFixtures("pog.gradek.data.json");                
            json = JSON.parse(f);
            dynamicMenuEngine = new DynamicMenuEngine.Engine(json.levelData);
    });
    
    it("1.1 bad guys factor1 length should be equal to json's factor1 length",()=>{
        console.log("Engine Facgtor",dynamicMenuEngine.menuElements.length);
        expect(dynamicMenuEngine.menuElements.length).toBe(json.levelData.length);
    });
   

    });*/
}